---
description: Show the Circle status dashboard for the current project
allowed-tools: Read, Grep, Glob, Bash
argument-hint: "[detailed]"
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/status/SKILL.md` and execute its workflow. Treat `$ARGUMENTS` as the status skill's arguments.
